[[共役勾配法]]の収束を早める．
条件数というのがあり，条件数が大きいほど悪条件．

$Ax=b$ において，$b$ の誤差に対する $x$ の誤差の比率．
$A$ は必ず正方行列になる? 数値計算で出てくる連立方程式はそうと言える．
- 別に正方行列でなくても良い

$Ax=b \rightarrow P^{-1}Ax = P^{-1}b$

$(P^{-1}A) x = c,~~ c= P^{-1}b$ を解く．$P^{-1}A$ の条件数が $A$ より小さくなっていれば，収束が早くなる．
$P^{-1}$ は反復法の各ステップで適用される．本当? 最初に計算するだけでいいのでは?
~~そうすると，$P^{-1}A$ をメモリに保存しておく必要があるため，疎行列でない場合に辛くなる．~~
- 残差に前処理行列を掛ける部分があるので，各ステップで計算が必要．

- ヤコビ前処理
- SOR前処理．

後で本で調べよう．


